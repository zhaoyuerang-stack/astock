# MODULE_STATUS

Status: ONLINE_CRITICAL

Role: Candidate promotion, phase1 anti-leakage audit, phase2 backtest, phase3 walk-forward, phase4 registry write, composite promotion, and 9-Gate runner.

Keep because: workflow is the only formal path from research candidate to registry.

Boundary:
- Must not be bypassed for official strategy admission.
- May consume factory outputs, engine results, governance checks, and registry APIs.
- Must not depend on `scripts/research` CLI wrappers.
